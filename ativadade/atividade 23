import os 


os.system("cls || clear")

while True:
    nota = float(input("digite a sua nota: "))

    if nota < 0 or nota > 10:
        print("Reprovado, nota não favoravel, tente novamente.\n")
    else:
        break
    

print(f"Nota informada: {nota}")
print(f"Aprovado!")
print("Fim.")